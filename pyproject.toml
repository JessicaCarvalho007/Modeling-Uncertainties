[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gw-uncertainty"
version = "0.1.0"
description = "Groundwater model uncertainty analysis tools."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
